# Workspace-level pubspec for the atomly monorepo. Not publishable.
name: atomly_workspace
description: Workspace root for the atomly + atomly_flutter packages.
publish_to: "none"
version: 0.0.0

environment:
  sdk: ^3.5.0

dev_dependencies:
  melos: ^6.0.0
