# atomly example

A minimal, headless Dart example that exercises the three most common
kinds of atom — value, computed, and async — through an `AtomStore`.

Run it from the package root:

```sh
dart run example/atomly_example.dart
```

For Flutter widgets and `BuildContext` extensions, see the
[`atomly_flutter` example](../../atomly_flutter/example).
